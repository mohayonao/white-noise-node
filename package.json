{
  "name": "white-noise-source",
  "description": "WhiteNoiseSourceNode for Web Audio API",
  "version": "1.0.0",
  "author": "Nao Yonamine <mohayonao@gmail.com>",
  "bugs": {
    "url": "https://github.com/mohayonao/white-noise-source/issues"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "istanbul": "^0.4.3",
    "mocha": "^2.5.3",
    "uglify-js": "^2.6.2",
    "web-audio-test-api": "^0.5.2"
  },
  "files": [
    "lib",
    "package.json",
    "README.md"
  ],
  "homepage": "https://github.com/mohayonao/white-noise-source/",
  "keywords": [
    "webaudioapi",
    "whitenoise"
  ],
  "license": "MIT",
  "main": "lib/WhiteNoiseSourceNode.js",
  "repository": {
    "type": "git",
    "url": "http://github.com/mohayonao/white-noise-source.git"
  },
  "scripts": {
    "build": "npm run build-browser && npm run build-minify",
    "build-browser": "browserify lib/WhiteNoiseSourceNode.js --standalone WhiteNoiseSourceNode -o build/white-noise-source.js",
    "build-minify": "uglifyjs build/white-noise-source.js -o build/white-noise-source.min.js",
    "cover": "istanbul cover node_modules/mocha/bin/_mocha",
    "gh-pages": "git checkout gh-pages && git merge master && git push origin gh-pages && git checkout master",
    "postversion": "git push && git push --tags",
    "prepublish": "npm run test && npm run build",
    "preversion": "npm run test",
    "test": "mocha",
    "travis": "npm run test",
    "version": "npm run build && git add -A build"
  }
}
